<% if (logged_in? && current_user.role?(:customer)) || !logged_in? %>
    <%= render partial: "partials/index_structure_2", locals: {model_name: "item", 
                                                         primary: "active_items", 
                                                         secondary: nil} %>
<% else %>
    <%= render partial: "partials/index_structure", locals: {model_name: "item", 
                                                         primary: "active_items", 
                                                         secondary: nil,
                                                         sidebar: "inactive_items"} %>
<% end %>